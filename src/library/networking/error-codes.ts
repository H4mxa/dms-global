/* eslint-disable */

const UNKNOWN_ERROR = 0;

// Token expired
const TOKEN_EXPIRED_ERROR = 5000;

// internal errors
const SERVER_ERROR = 190001;

export default {
  TOKEN_EXPIRED_ERROR,

  UNKNOWN_ERROR,

  // server error
  SERVER_ERROR,
};
